# Memory & CLAUDE.md

Index for this concept. Each numbered subfolder is a self-contained example
you `cd` into before running `claude` — see the root README for why.

## Examples

| # | Sub-example | Status |
|---|---|---|
| 01 | [Basics](01-basics/) — root CLAUDE.md plus nested loading via a touched subdirectory | done |

## Adding more depth here

More memory & claude.md material goes in a new `0N-topic/` subfolder here,
not a new top-level `examples/` number — see the root `CLAUDE.md`
conventions.
