---
description: A minimal example slash command that echoes back an argument.
---
Say a short, friendly hello to $ARGUMENTS (or "there" if no argument was
given), then mention that this text came from `.claude/commands/hello.md`.
