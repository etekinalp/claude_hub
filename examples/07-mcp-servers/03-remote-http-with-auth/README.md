# 03 — Remote HTTP Server with Auth (planned)

## What this will demonstrate
An MCP server reached over HTTP rather than local stdio (`"type": "http"` in
`.mcp.json`), including an auth header/token, as a contrast to 01's local
subprocess server.

## Open question before building
Needs a real (or mock) remote MCP endpoint to test against — a public demo
server, or a small one we stand up ourselves. Decide this when we get here.
