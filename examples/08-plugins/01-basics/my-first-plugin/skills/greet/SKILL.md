---
name: greet
description: Use this skill when the user asks the plugin to greet them or introduce itself. Confirms this text is running from the my-first-plugin plugin, not this project's own .claude/skills.
---
Greet the user by name if you know it, and explicitly say this response came
from the `my-first-plugin` plugin's `greet` skill (via `--plugin-dir`), to
make clear this isn't one of the project's own `.claude/skills`.
