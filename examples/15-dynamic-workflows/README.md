# Dynamic Workflows

Index for this concept. Each numbered subfolder is a self-contained example
you `cd` into before running `claude` — see the root README for why.

## Examples

| # | Sub-example | Status |
|---|---|---|
| 01 | [Basics](01-basics/) — saved fan-out workflow finding TODOs across sample files, plus the ad-hoc form | done |

## Adding more depth here

More dynamic-workflows material goes in a new `0N-topic/` subfolder here,
not a new top-level `examples/` number — see the root `CLAUDE.md`
conventions.
