This folder is empty on purpose.

Only add `agents/`, `commands/`, `hooks/`, `skills/`, or `settings.json` here if
*this specific example* needs them. Don't copy config in from other examples —
each example's `.claude/` should contain only what it's actually demonstrating.
