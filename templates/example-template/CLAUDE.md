<!--
Optional: only keep this file if the example is actually about memory/CLAUDE.md
behavior, or genuinely needs project context loaded. Delete it if the concept
doesn't need it — don't cargo-cult an empty CLAUDE.md into every example.
-->
